<!DOCTYPE html>
<html lang="en">
  <%- include("../partials/head.ejs") %>

  <body style="font-family:cursive;">
    <%- include("../partials/header.ejs") %>
    <main>
      <div>

        <article>
          <h1>Animal Name:<%= animal.name %> </h1>
          <h1>Species:<%= animal.species %></h1>
          <h1>Animal Location:<%= animal.location %></h1>
          <h1>Animal Life Expectancy:<%= animal.lifeExpectancy%></h1>
         <h1>Extinct?<%= animal.extinct ? "Yes" : "No" %></h1> 
          <a href="/animals/<%= animal._id %>/edit"><button>Edit</button></a> <br><br>
          <form action="/animals/<%= animal._id %>?_method=DELETE" method="POST">
            <input type="submit" value="Delete"/>
           
          </form>
          <a href="/animals/"><button>Back to Main</button></a>
        </article>

      </div>
    </main>
  </body>
</html>